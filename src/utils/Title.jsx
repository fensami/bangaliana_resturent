
const Title = ({title}) => {
  return (
    <div>
      <h1 className="text-center font-bold text-5xl alltext-shadow  text-[#028a3a] py-5">
        {title}
      </h1>
    </div>
  );
};

export default Title;